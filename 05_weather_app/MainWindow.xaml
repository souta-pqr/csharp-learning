<Window x:Class="_05_weather_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="千葉県 天気アプリ" Height="580" Width="360"
        ResizeMode="NoResize"
        Background="#1e1e2e">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 都市選択 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="CityBox"
                      Grid.Column="0"
                      FontSize="14"
                      Padding="8,6"
                      SelectionChanged="CityBox_SelectionChanged"/>
            <Button Grid.Column="1"
                    Content="更新"
                    FontSize="13"
                    Margin="8,0,0,0"
                    Background="#7c3aed"
                    Foreground="White"
                    BorderThickness="0"
                    Click="RefreshButton_Click"/>
        </Grid>

        <!-- 現在の天気 -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,12">
            <TextBlock x:Name="IconText"
                       Text="🌡"
                       FontSize="60"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="TempText"
                       Text="--°C"
                       FontSize="46"
                       FontWeight="Light"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="ConditionText"
                       Text="読み込み中..."
                       FontSize="16"
                       Foreground="#a0a0c0"
                       HorizontalAlignment="Center"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <!-- 湿度・風速 -->
        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#2a2a3e" CornerRadius="8" Margin="0,0,5,0" Padding="10,8">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="💧 湿度" Foreground="#a0a0c0" FontSize="11" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="HumidityText" Text="--%"
                               Foreground="White" FontSize="18" FontWeight="SemiBold"
                               HorizontalAlignment="Center" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#2a2a3e" CornerRadius="8" Margin="5,0,0,0" Padding="10,8">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="💨 風速" Foreground="#a0a0c0" FontSize="11" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="WindText" Text="-- km/h"
                               Foreground="White" FontSize="18" FontWeight="SemiBold"
                               HorizontalAlignment="Center" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 時間別予報 -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Text="今日の時間別予報"
                       Foreground="#a0a0c0"
                       FontSize="12"
                       Margin="0,0,0,6"/>
            <ListBox x:Name="HourlyList"
                     Grid.Row="1"
                     Background="#2a2a3e"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3a3a5e"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="28"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Time}"
                                       Foreground="#a0a0c0" FontSize="13"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Icon}"
                                       FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Display}"
                                       Foreground="White" FontSize="13"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
